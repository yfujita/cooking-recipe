これはレシピをmarkdown形式で保管するリポジトリ。

## ディレクトリ構成

料理のジャンルごとにディレクトリを分けて、その中にレシピのmarkdownファイルを置く。

* `maindish/` - 主菜
* `ricebawl/` - 丼もの
* `salad/` - サラダ
* `sidedish/` - 副菜・小鉢
* `soup/` - スープ・汁物

新しいジャンルのレシピを追加する際は、既存のジャンルに当てはまらない場合のみ新しいディレクトリを作成する。

各ジャンルディレクトリの中に、そのレシピで使う画像を置く `images/` ディレクトリを作ってよい（例: `ricebawl/images/`）。

## レシピファイルの命名規則

* ファイル名は料理名をそのまま日本語で付ける（例: `カツオのネバネバ丼.md`）。
* 画像ファイルはローマ字のスネークケースで命名する（例: `katsuo_nebaneba.jpeg`）。

## レシピファイルのフォーマット

既存のレシピ（[ricebawl/カツオのネバネバ丼.md](ricebawl/カツオのネバネバ丼.md)）を参考に、以下の構成で記述する。

```markdown
# 料理名

![料理名](images/xxx.jpeg)

料理の簡単な説明（1〜2行）。

## 材料

### カテゴリ名（例: メインの具材、タレなど）

* 材料名
* 材料名

---

## 作り方

### 1. 手順の見出し

1. 具体的な手順。**強調したい分量や時間は太字**にする。
2. ...

### 2. 次の手順の見出し

...

### N. 盛り付ける

丼・皿にご飯や具材を盛り付けて完成、といった最終手順で締める。
```

### 記述時のポイント

* 材料は用途ごとに `###` の見出しでグループ分けする（例: タレ用、具材用など）。
* 作り方の各手順は `### N. 見出し` で区切り、その下に番号付きリストで具体的な作業を書く。
* 分量・加熱時間など重要な数値情報は `**太字**` で強調する。
* レシピに写真がある場合は、タイトル直下（説明文の前）に `![料理名](images/ファイル名)` の形式で画像を挿入する。

## その他

* README.mdはリポジトリの簡単な概要のみを記載し、詳細な構成はこのCLAUDE.mdに記載する。


